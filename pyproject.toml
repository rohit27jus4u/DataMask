[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "datamask"
version = "0.1.0"
dependencies = [
    "pyspark",
    "pandas"

]

[tool.setuptools.packages.find]
where = ["src"]


